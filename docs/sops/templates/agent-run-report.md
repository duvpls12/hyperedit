# Agent Run Report

> Fill this template when a stage completes, fails, or blocks. Submit to Orchestrator.

---

## Run Metadata

| Field | Value |
|-------|-------|
| **Project ID** | `<project_id>` |
| **Stage** | `<stage_name>` (e.g., `footage-intake`, `audio`, `assembly`) |
| **Agent Skill** | `<skill_name>` (e.g., `hyperedit-agent-footage-intake-sort`) |
| **Run Date** | `<YYYY-MM-DD HH:MM>` |
| **Duration** | `<minutes>` |
| **Outcome** | `DONE` / `DONE_WITH_WARNINGS` / `BLOCKED` |

---

## Outcome Summary

<!-- One sentence: what was accomplished or what failed -->

---

## Artifacts Written

| Artifact | Path | Status | Schema Valid |
|----------|------|--------|--------------|
| `<nn_artifact_name.json>` | `state/agents/<project_id>/` | pass / warn / fail | yes / no |
| `<nn_artifact_name.json>` | `state/agents/<project_id>/` | pass / warn / fail | yes / no |
| `<nn_artifact_name.json>` | `state/agents/<project_id>/` | pass / warn / fail | yes / no |

---

## Quality Gate Results

| Gate | Result | Detail |
|------|--------|--------|
| `<gate_name>` | pass / warn / fail | `<specific value or failure reason>` |
| `<gate_name>` | pass / warn / fail | `<specific value or failure reason>` |

---

## Blocking Issues

<!-- List any blocking_issues[] that remain unresolved. "None" if all clear. -->

```
- <issue 1>
- <issue 2>
```

---

## Warnings (Non-Blocking)

<!-- List any warn-status issues that were accepted and documented. "None" if all clear. -->

```
- <warning 1>
- <warning 2>
```

---

## Fallbacks Invoked

<!-- List any fallback ladder steps taken (vision model demotion, GPU queue, etc.) -->

```
- <fallback action 1> (reason: <why>)
- <fallback action 2> (reason: <why>)
```

---

## Retry Log

| Attempt | Approach | Outcome |
|---------|----------|---------|
| 1 | `<what was tried>` | pass / fail |
| 2 | `<different approach>` | pass / fail |

---

## Assumptions Made

<!-- List any assumptions that were not in the brief but were required to proceed -->

```
- <assumption 1>
- <assumption 2>
```

---

## Open Questions for Orchestrator

<!-- Decisions needed from Orchestrator or user to unblock or clarify -->

```
- <question 1>
- <question 2>
```

---

## Next Stage Handoff

| Field | Value |
|-------|-------|
| **Next Stage** | `<stage_name>` |
| **Unblocked by this run** | yes / no |
| **Conditional flags** | `photo-to-video: enabled/skipped`, `text-graphics: enabled/skipped` |
| **Key artifact for next stage** | `<nn_artifact.json>` |

---

## Tool Invocations

<!-- List all external tools/APIs called with endpoint and result summary -->

| Tool | Endpoint / Command | Result |
|------|--------------------|--------|
| FFmpeg server | `POST /session/{id}/assets` | 24 clips uploaded |
| LM Studio (11B) | Vision classification | 22/24 clips tagged |
| ComfyUI | `POST /prompt` workflow_id=`<id>` | 3 variants generated |

---

## Source References

<!-- Files consulted during this run -->

```
- skills/hyperedit-agent-<stage>/SKILL.md
- skills/hyperedit-agent-<stage>/SOP.md
- skills/hyperedit-agent-<stage>/CONTEXT.md
- state/agents/<project_id>/00_project_brief.json
```

---

*Generated by `skills/hyperedit-agent-<stage>/` | HyperEdit Pipeline*
